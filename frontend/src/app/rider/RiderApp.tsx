'use client';

// TODO(phase-5): main state container — login / orders / detail / profile routes,
// localStorage hydration, GPS hook wiring, periodic order fetch.

export default function RiderApp() {
  return null;
}
